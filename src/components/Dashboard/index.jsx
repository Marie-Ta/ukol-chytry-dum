import React from 'react';
import Lights from '../Lights';

const Dashboard = () => {
    return(
        <main className="dashboard">
            <Lights/>
        </main>
    );
}

export default Dashboard;